language: cpp
compiler:
  - gcc
  - clang
  
# Required packages
before_install:
 - sudo apt-add-repository -y ppa:andykimpe/cmake3
 - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
 - sudo add-apt-repository -y ppa:mapnik/boost-backports-1-54
 - sudo apt-get update -qq
 
install:
 - sudo apt-get install -y -qq boost1.54
 - sudo apt-get install -y -qq gcc-4.8 g++-4.8
 - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 90
 - sudo apt-get install -y -qq cmake3
 
# configuration
before_script:
  - mkdir build && cd build
  - cmake --version
  - cmake .. -DCMAKE_BUILD_TYPE:STRING=Debug

# make & check
script:
  - make 
  - make test
